for run this project, on linux, it's necessary to install a library to access the bluetooth

sudo apt-get install libbluetooth-dev

also my bluetooth was soft blocked, so I run 

sudo rfkill unblock bluetooth